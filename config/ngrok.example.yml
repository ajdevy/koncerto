version: "2"
tunnels:
  koncerto-dashboard:
    proto: http
    addr: ${NGROK_ADDR:-localhost:17348}
    inspect: false
    # --- OAuth Authentication ---
    # Uncomment ONE provider below and configure allowed emails/domains.
    #
    # Option A: Google OAuth
    # oauth:
    #   provider: google
    #   allow_emails:
    #     - admin@yourdomain.com
    #   allow_domains:
    #     - yourdomain.com
    #
    # Option B: GitHub OAuth
    # oauth:
    #   provider: github
    #   allow_emails:
    #     - admin@yourdomain.com
    #   allow_domains:
    #     - yourdomain.com
